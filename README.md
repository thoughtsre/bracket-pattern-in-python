> **This repo has been migrated to [GitHub](https://github.com/thoughtsre/bracket-pattern-in-python).**

# Bracket Pattern in Python

The bracket pattern is a very useful coding pattern that ensures that resources are properly closed and enables developers to perform custom actions upon errors and resource closure.

This is a demo repo to show how to use the `contextmanager` to achieve this in Python.